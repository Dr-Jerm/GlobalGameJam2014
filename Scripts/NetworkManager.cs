using UnityEngine;
using System.Collections;

public class NetworkManager : MonoBehaviour {

	float botX = 0.1F;
	float botY = 0.1F;
	float botW = 0.1F;
	float botH = 0.1F;

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		
	}

	void OnGUI() {
		Rect r1 = new Rect(this.botX, this.botY, this.botH, this.botW);
		Rect r2 = new Rect)
}

