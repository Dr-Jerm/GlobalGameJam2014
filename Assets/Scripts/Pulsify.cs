using UnityEngine;
using System.Collections;

public class Pulsify : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Light l = this.GetComponent<Light>();
		Debug.Log (l);
	}
	
	// Update is called once per frame
	void Update () {
	}
}